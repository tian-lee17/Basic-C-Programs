/*The printf() function is an output function defined in the standard input/output header file (stdio.h). 
Its primary purpose is to print (display) formatted output to the standard output stream, which is typically the console or terminal screen.*/
//Syntax: printf("Format string",arg1,arg2,...);
#include<stdio.h>
int main()
{
  printf("HelloWorld!");
  return 0;//ensures the successful execution of the code.
}
